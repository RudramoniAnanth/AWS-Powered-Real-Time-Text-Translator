# Scripts

Build and deployment scripts for the project.

## Available Scripts

Currently, all scripts are defined in `package.json`:

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run preview` - Preview production build
- `npm run lint` - Run ESLint

## Future Scripts

This folder is reserved for custom build and deployment scripts.
